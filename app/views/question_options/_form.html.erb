<%= form_with(model: question_option, local: true) do |form| %>
  <% if question_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_option.errors.count, "error") %> prohibited this question_option from being saved:</h2>

      <ul>
      <% question_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field d-none">
    <%= form.label :question_id %>
    <%= form.text_area :question_id, id: :question_option_question_id %>
  </div>

  <div class="field">
    <%= form.label :value,'Valor' %>
   <%= form.select :value , options_for_select([["Correcta", "1"], ["Incorrecta", "0"]]) %> 
  </div>

  <div class="field">
    <%= form.label :description,'Descripcion' %>
    <%= form.text_area :description , id: :question_option_description%>  
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
