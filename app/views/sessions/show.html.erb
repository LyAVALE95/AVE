<p id="notice"><%= notice %></p>
<div class="card yellow col-md-2">
            <div class="card-content black-text">
              <span class="card-title"><%= @session.price %> </span>
            </div>
          </div>

<h1><%= @session.name %></h1>
 <% @details.each do |d| %>
<div class="row">
        <div class="col s12 m6">
        
          <div class="card blue-grey darken-1">
            <div class="card-content white-text">
              <span class="card-title"><%= d.txt1%></span>
              <p><%= d.txt2%> </p>
            </div>
            <div class="card-action">
              <%= link_to "Ver", controller: "session_details", action: "show", id: d.id  %>
              <a href="#">Ir a los ejercicios</a>
            </div>
          </div>
        </div>
      </div>
<%end %>
<%= link_to 'Edit', edit_session_path(@session) %> |
<%= link_to 'Back', sessions_path %>
