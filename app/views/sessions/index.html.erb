<p id="notice"><%= notice %></p>

<h1>Lecciones (Temas)</h1>
<%if current_user.rol == "t"%>
<%= link_to 'Nueva Leccion', new_session_path,:class => 'btn yellow btn-warning' %>
<%end%>
<!--<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Avaible</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sessions.each do |session| %>
      <tr>
        <td><%= session.name %></td>
        <td><%= session.avaible %></td>
        <td><%= session.price %></td>
        <td><%= link_to 'Show', session %></td>
        <td><%= link_to 'Edit', edit_session_path(session) %></td>
        <td><%= link_to 'Destroy', session, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
-->
<% if @sessions.size == 0 %>
<h4 class="grey-text">Aún no tiene lecciones</h4>
<%end%>
  <ul class="collection">
   <% @sessions.each do |session| %>
      <li class="collection-item avatar">
      <%if current_user.rol == "t"%>
      <img src="/pencil.png" alt="" class="circle">
      <h6><%= link_to "Ver " + session.name, session, :onclick => "goandrefresh3()" %></h6>
      <%else%>
        <%if session.price && session.price.to_i <= current_user.score.to_i%>
         <img src="/pencil.png" alt="" class="circle">
         <h6><%= link_to "Ver " + session.name, session, :onclick => "goandrefresh3()" %></h6>        
        <%else%>
         <img src="/lock.png" alt="" class="circle">
        <h6 class="grey-text text-darken-1"><strike><%= session.name %></strike></h6>       
        <%end%>
      <%end%>
        <p><%= session.avaible %></p>
        <p><%= session.price %></p>
        <p><%if current_user.rol == "t"%>
        <%= link_to 'Editar', edit_session_path(session) %>        
        |<%= link_to 'Eliminar', session, method: :delete, data: { confirm: 'Are you sure?' } %><%end%></p>
      </li>
    <% end %>
  </ul>
<br>